{
  "name": "termuxhost-runtime",
  "version": "1.0.0",
  "private": true,
  "description": "Runtime dependencies for TermuxHost API — Termux/Android deployment",
  "scripts": {
    "start": "node ../dist/index.mjs",
    "pm2": "pm2 start ../dist/index.mjs --name termuxhost-api"
  },
  "dependencies": {
    "nodemailer": "^6.9.16"
  },
  "optionalDependencies": {
    "@ngrok/ngrok": "^1.4.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
